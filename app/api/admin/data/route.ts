import {cookies} from "next/headers";
import {createHmac} from "crypto";
import {games} from "../../../../data/games";
import {staff} from "../../../../data/team";
function token(){return createHmac("sha256",process.env.ADMIN_SESSION_SECRET||"").update("spider-admin").digest("hex")}
async function ok(){const c=await cookies();return !!process.env.ADMIN_SESSION_SECRET&&c.get("spider_admin")?.value===token()}
async function githubPut(path:string,content:string,message:string){const token=process.env.GITHUB_TOKEN,repo=process.env.GITHUB_REPO||"kambreyacc-sketch/spider-studios";if(!token)throw new Error("GITHUB_TOKEN is not configured");const base="https://api.github.com/repos/"+repo+"/contents/"+path;const current=await fetch(base,{headers:{Authorization:"Bearer "+token,Accept:"application/vnd.github+json"}});let sha;if(current.ok)sha=(await current.json()).sha;const body:any={message,content:Buffer.from(content).toString("base64"),branch:"main"};if(sha)body.sha=sha;const r=await fetch(base,{method:"PUT",headers:{Authorization:"Bearer "+token,Accept:"application/vnd.github+json","Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text())}
export async function GET(){if(!(await ok()))return Response.json({error:"Unauthorized"},{status:401});return Response.json({games,staff})}
export async function POST(req:Request){if(!(await ok()))return Response.json({error:"Unauthorized"},{status:401});try{const {kind,data}=await req.json();if(kind==="games"){const content="export type Game={title:string;description:string;image:string;url:string;tag:string;featured?:boolean;sale?:boolean;status?:string;robloxId?:string;discord?:string;releaseDate?:string;updatedAt?:string;visits?:string;players?:string;favorites?:string;screenshots?:string[]};\n\nexport const games:Game[]="+JSON.stringify(data,null,2)+";\n";await githubPut("data/games.ts",content,"Update games from Spider Studios admin")}else if(kind==="staff"){const content="export type StaffMember={id:string;name:string;role:string;department:string;bio?:string;avatar_url?:string;avatar_source?:string;discord_avatar_url?:string;discord_id?:string;verified?:boolean;roblox_url?:string;social_links?:{platform:string;url:string}[]};\nexport const staff:StaffMember[]="+JSON.stringify(data,null,2)+";\n";await githubPut("data/team.ts",content,"Update staff from Spider Studios admin")}else return Response.json({error:"Invalid type"},{status:400});return Response.json({ok:true})}catch(e){return Response.json({error:e instanceof Error?e.message:"Save failed"},{status:500})}}